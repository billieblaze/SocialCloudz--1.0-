version					= 3.0

# Mybatis 
mybatis.path 			= \\common\\mybatis-migrations-3.0.6\\bin
mybatis.profile 		= development

# ColdBox deploy	
deploytag.path 			= config\\_deploy.tag
    
# subversion paths
svn.path				= http://www.scdevsite.com/svn/${ant.project.name}/
svn.trunk				= ${svn.path}trunk/
svn.staging				= ${svn.path}branches/staging/
svn.tags				= ${svn.path}tags/

# paths
path.cfxtest			= w:
path.development		= ${path.cfxtest}\\${ant.project.name}
#path.staging			= /mnt/testunc/cfusion.ear/cfusion.war/${ant.project.name}
#path.production			= /mnt/apps/${ant.project.name}

# local paths
path.build				= ../../build/
path.temp				= ${path.build}temp
path.dist				= ${path.build}${ant.project.name}
path.local				= ${path.temp}${ant.project.name}

# testing
junit.out.dir.xml  		= build/testresults
junit.out.dir.html		= build/testresults/html
output.dir				= tmp/
style.dir				= build/xsl/
web.root				= /socialcloudz/

dev.url					=  127.0.0.1
dev.directory			= /socialcloudz
dev.port				= 80

#stage.url				=  testunc.myresearchonline.org
#stage.directory			= /${ant.project.name}
#stage.port				= 80

#production.url				=  apps.research.unc.edu
#production.directory			= /${ant.project.name}
#production.port				= 80